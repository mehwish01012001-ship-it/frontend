export const generateTitle = (title) => `${title} | RQ Fashion`;

export const generateDescription = (description) =>
  description || 'Discover premium fashion and curated collections at RQ Fashion.';
